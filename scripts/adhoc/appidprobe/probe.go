// Package appidprobe is a throwaway used to validate that a bot's fix commit
// is attributed to the GitHub-App bot identity. Deleted after validation.
package appidprobe

// Double returns twice its input.
func Double(n int) int { return n * 2 }
